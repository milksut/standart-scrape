you need to downland node.js and pupteer 
(if file has the node_modules file , package.json and package-lock.json files you dosent need to downland)
when you downland both of them just open the file in console and input
node index
then just enter
to scrape YOUR info from edestek2 you need to input your information
this information only contained in a json created in this file and dosent used anywhere else
information is stored inside json files an you are only notifed when its changed(edestek2 information always shows)
